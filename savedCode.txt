<% allAreas.forEach(area => { %>
    <li>
        <%= area.name %> <br>
        <a href="/reports/<%= area.id %>">Trip Reports</a> <br>
        <a href="/areas/<%= area.id %>/current">Current Weather</a>
        <!-- remove the forecast link before finished -->
        <a href="/areas/<%= area.id %>/forecast">Forecast</a>
    </li>
<% }) %>

-- above was from home page


<label for="report">Trip Report: </label>
        
        <input
            type="text"
            class="report-box" 
            id="report"
            name="report"
            value="<%= report.report %>"
            required
        />

        <input type="submit" value="Submit Changes">

-- from edit page


<form action="/reports/<%= report.id %>/?_method=PUT" method="POST">
        
    <div class="report-container mb-3 w-75">
        <label for="report" class="form-label">Trip Report:</label>
        <textarea type="text" name="report" id="report" class="form-control" rows="3" value="<%= report.report %>" ></textarea>
        <input type="submit" class="btn btn-primary">        
    </div>

</form>